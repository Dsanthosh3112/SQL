Here’s a **Cricket Tournament Management System** SQL project:

```sql
CREATE TABLE Players (
    PlayerID INT PRIMARY KEY,
    FullName VARCHAR(100),
    Team VARCHAR(50),
    Position VARCHAR(50)
);

CREATE TABLE Matches (
    MatchID INT PRIMARY KEY,
    Date DATE,
    Venue VARCHAR(100),
    TeamA VARCHAR(50),
    TeamB VARCHAR(50),
    Winner VARCHAR(50)
);

CREATE TABLE Scorecards (
    ScorecardID INT PRIMARY KEY,
    MatchID INT,
    PlayerID INT,
    Runs INT,
    Wickets INT,
    FOREIGN KEY (MatchID) REFERENCES Matches(MatchID),
    FOREIGN KEY (PlayerID) REFERENCES Players(PlayerID)
);

-- Inserting sample data for Players
INSERT INTO Players VALUES 
(1, 'Virat Kohli', 'India', 'Batsman'),
(2, 'Rohit Sharma', 'India', 'Batsman'),
(3, 'David Warner', 'Australia', 'Batsman'),
(4, 'Steve Smith', 'Australia', 'Batsman');

-- Inserting sample data for Matches
INSERT INTO Matches VALUES 
(101, '2024-03-01', 'Wankhede Stadium', 'India', 'Australia', 'India'),
(102, '2024-03-05', 'Melbourne Cricket Ground', 'India', 'Australia', 'Australia');

-- Inserting sample data for Scorecards
INSERT INTO Scorecards VALUES 
(1, 101, 1, 50, 0),
(2, 101, 2, 70, 0),
(3, 102, 3, 30, 1),
(4, 102, 4, 40, 2);

-- Query to get all players who scored runs in a match
SELECT p.FullName, s.Runs, s.Wickets, m.Date
FROM Scorecards s
JOIN Players p ON s.PlayerID = p.PlayerID
JOIN Matches m ON s.MatchID = m.MatchID
WHERE s.Runs > 0;

-- Query to get the winner of each match
SELECT m.Date, m.Venue, m.Winner 
FROM Matches m;

-- Query to update the runs scored by a player in a match
UPDATE Scorecards SET Runs = 80 WHERE ScorecardID = 2;

-- Query to update the match winner
UPDATE Matches SET Winner = 'India' WHERE MatchID = 102;

-- Query to delete a scorecard entry
DELETE FROM Scorecards WHERE ScorecardID = 3;
```


